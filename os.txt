sudo apt update
sudo apt upgrade
sudo apt install git -y
git clone https://github.com/MathiMuts/sample-camera.git
cd sample-camera/
sudo apt install python3 python3-pip python3-virtualenv python3-tk -y
sudo apt install --no-install-recommends xserver-xorg -y
sudo apt install libgl1 openbox xterm xinit -y
# `sudo nano /boot/firmware/config.txt` > add lines `hdmi_force_hotplug=1` and `hdmi_mode=82`
# `nano ~/.xinitrc` > create and add the lines: `#!/bin/sh`, `xset s off -dpms`, `cd /home/mca/sample-camera` and `exec /home/mca/sample-camera/venv/bin/python -m sample_calibrator`
sudo apt purge unclutter
python3 -m velv venv
pip install -r requirements.txt

sudo mkdir -p /etc/systemd/system/getty@tty1.service.d
# sudo nano /etc/systemd/system/getty@tty1.service.d/override.conf > write the lines: `[Service]`, `ExecStart=`, `ExecStart=-/sbin/agetty --autologin mca --noclear %I $TERM`
# nano /home/mca/.bash_profile > write lines (without ```):
    ```
    #!/bin/bash
    if [[ -z $DISPLAY ]]; then
        sleep 2
        startx
    fi
    ```

cd ~
NOTE: Mount de netwerkdrive!
sudo reboot